{
  "name": "codenail-api",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:production": "NODE_ENV=production serverless deploy --stage production",
    "logs:production": "NODE_ENV=production serverless logs --stage production -f",
    "start": "serverless offline start --port 8000 --prefix dev",
    "config-for-production": "NODE_ENV=production node config/index.js",
    "config-for-development": "NODE_ENV=development node config/index.js"
  },
  "author": "Francesco Agosti",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^3.14.0"
  },
  "dependencies": {
    "aws-sdk": "^2.58.0",
    "stripe": "^4.22.0"
  }
}
