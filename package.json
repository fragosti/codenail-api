{
  "name": "codenail-api",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:production": "env-cmd ./.env_prod serverless deploy --stage production",
    "start": "env-cmd ./.env_dev serverless offline start --port 8000 --prefix dev"
  },
  "author": "Francesco Agosti",
  "license": "ISC",
  "devDependencies": {
    "env-cmd": "^5.1.0",
    "serverless-offline": "^3.14.0"
  },
  "dependencies": {
    "aws-sdk": "^2.58.0",
    "stripe": "^4.22.0"
  }
}
